<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="herisson.application.br.speakforme">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.SplashScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.CadastroUsuarioActivity" />
        <activity android:name=".Activity.HomeUser" />
        <activity android:name=".Activity.HomeAdmin" />
        <activity android:name=".Activity.EdicaoExpressao" />
        <activity android:name=".Activity.EdicaoIdiomas" />
        <activity android:name=".Activity.EdicaoUsuario" />
        <activity android:name=".Activity.InsercaoExpressao" />
        <activity android:name=".Activity.InsercaoIdiomas" />
        <activity android:name=".Activity.ListarUsuarios" />

    </application>

</manifest>